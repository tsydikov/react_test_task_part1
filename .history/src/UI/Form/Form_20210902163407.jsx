import React from 'react'
import cl from "./Form.module.css";

const Form = ({ children }) => {
    return (
        <div className={}>
            {children}
        </div>


    )
}

export default Form;